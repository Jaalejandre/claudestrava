# ── macOS / SMB cruft ───────────────────────────────
.DS_Store
._*
.AppleDouble
.LSOverride
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems
*.swp
Thumbs.db

# ── Obsidian: config y plugins (re-instalables, y hay node_modules pesados) ──
.obsidian/
**/.obsidian/

# ── Estado de herramientas / agentes (por-máquina, efímero) ──
.claudian/
.claude/
.agents/
.opencode/
.copilot/
.cursor/

# ── Copilot: cache y conversaciones (NUNCA versionar: log 2026-09-12
#    API key de Anthropic filtrada en una conversación → push bloqueado) ──
copilot/.model-catalog-cache.json
copilot/**/node_modules/
copilot/copilot-conversations/

# ── Ruido en la raíz ────────────────────────────────
Screenshot *.png
Untitled*.canvas
