# Tailscale Configuration Audit Report
**Date**: 2026-09-17  
**Status**: ❌ BROKEN — TUN kernel module missing

---

## CURRENT STATUS

| Component | Status | Details |
|-----------|--------|---------|
| **Tailscale binary** | ✅ Installed | v1.102.4 |
| **tailscaled service** | ❌ CRASHING | Module tun not found |
| **Network interface** | ❌ MISSING | tailscale0 device cannot be created |
| **Authentication** | ⚠️ UNKNOWN | Never gets far enough to authenticate |

---

## ROOT CAUSE: Kernel TUN Module Missing

**Error logs:**
```
is CONFIG_TUN enabled in your kernel? `modprobe tun` failed with: 
modprobe: FATAL: Module tun not found in kernel
```

**Why it fails:**
- Tailscale needs the `tun` kernel module to create a virtual network interface (`tailscale0`)
- The CT 109 kernel (6.17.2-1-pve) does NOT have TUN compiled in
- This is a **Proxmox LXC container limitation** — TUN support must be enabled at Proxmox level

---

## INSTALLATION STATUS (Other CTs)

| CT | Tailscale | Status |
|----|-----------|--------|
| **109 (claude-dev)** | ✅ Installed | ❌ Crashing (TUN missing) |
| **901 (ubuntu)** | ❌ Not installed | — |
| **103 (ollama)** | ❌ Not installed | — |
| **100 (npm)** | ❌ Not installed | — |
| **108 (cloudflared)** | ❌ Not installed | — |

---

## NETWORK DIAGNOSTICS (CT 109)

**DERP latency** (Tailscale relay servers):
- Dallas (nearest): 36.3ms ✓
- Denver: 45.9ms ✓
- Chicago: 51.8ms ✓

**External IP**: 187.190.189.68:6083  
**UDP**: Enabled ✓  
**IPv4**: Yes ✓  
**IPv6**: Not available  

⚠️ **Network connectivity is FINE — only the TUN module is missing.**

---

## FIXES (In Order of Preference)

### Option A: Enable TUN in Proxmox (RECOMMENDED)
**Effort**: ~5 min (SSH to Proxmox host)

1. Edit CT 109 config on Proxmox:
   ```bash
   ssh root@192.168.0.52
   nano /etc/pve/lxc/109.conf
   ```

2. Add these lines:
   ```
   lxc.cgroup2.devices.allow: c 10:200 rwm
   lxc.cap.drop: 
   lxc.cap.keep: sys_admin net_admin
   ```

3. Restart CT:
   ```bash
   pct reboot 109
   ```

4. Test Tailscale:
   ```bash
   systemctl restart tailscaled
   tailscale up
   ```

**Result**: Tailscale will work in CT 109.

---

### Option B: Don't Use Tailscale in LXC
**Effort**: N/A

If Proxmox LXC doesn't support TUN, use Cloudflare tunnel (already working) instead.

---

### Option C: Run Tailscale on Proxmox Host
**Effort**: ~10 min

Install Tailscale on the Proxmox **host** (192.168.0.52) instead of in CT 109:
```bash
ssh root@192.168.0.52
curl -fsSL https://tailscale.com/install.sh | sh
tailscale up
```

**Result**: Host can VPN, containers relay through it (less secure, but works).

---

## RECOMMENDATION

**Use Option A** (Enable TUN in CT 109):
- Cleanest solution
- No host-level changes needed
- Tailscale runs isolated in container
- Full feature parity

Then:
1. José approves & authorizes with Tailscale account
2. Runs `tailscale up` → browser login
3. Adds other CTs if needed

---

## CURRENT CONFIGURATION FILES

**CT 109 tailscale config** (doesn't exist yet — needs auth):
- State: `/var/lib/tailscale/tailscaled.state` (empty)
- Socket: `/run/tailscale/tailscaled.sock` (fails to create)

**Service**:
- Unit: `/usr/lib/systemd/system/tailscaled.service`
- Enabled: Yes
- Status: Failing (restart counter at 4)

---

## NEXT STEPS

1. **Choose fix** (Option A recommended)
2. **Execute fix**
3. **Test**: `systemctl status tailscaled` + `tailscale status`
4. **Authenticate** if needed
5. **Verify** all CTs can reach each other via Tailscale (if installed on multiple)

---

**Verdict**: Tailscale is installed but **kernel module support is missing at the Proxmox level**. Not a configuration issue — a platform limitation.

---

**Generated by**: SatanZote AI (2026-09-17 12:53 CDMX)
